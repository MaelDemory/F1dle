node_modules
build
.git
.gitignore
README.md
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
Dockerfile
.dockerignore

# Documentation and orchestration: never referenced by the Dockerfile, so they
# only bloat the build context. nginx.conf.template is NOT listed — the
# production stage copies it.
docs
docker-compose.yml
fly.toml
